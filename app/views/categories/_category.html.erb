
<div id="<%= dom_id category %>">
  <h2>
    <strong>Category:</strong>
    <%= category.name %>
  </h2>

  <% category.items.each do |item| %>
    <p>
    <%= item.title %> | <%= item.price %> | <%= link_to "View Item", item %>
    </p>
    <% end %>
</div>