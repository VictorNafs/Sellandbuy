<%= link_to "Categories", categories_path %>

<h1>Items</h1>

<% if user_signed_in? %>
  <%= link_to "New item", new_item_path %>
<% end %>

<div id="items">
  <% @items.each do |item| %>
    <%= render item %>

    <p><%= link_to 'Show this item', item %></p>
    <p></p>
  <% end %>
</div>
